services:
  - type: web
    name: federal-contract-command-center
    runtime: node
    plan: free
    buildCommand: node --version
    startCommand: node server.js
    healthCheckPath: /health
    autoDeploy: true
